## what is the memory leaks
Once the garbage collector can't free up the memory from unused variables that slow down the performance thats called memory leaks
_________________________________________________
# example
  function greatings(){
    this.greating = "Hello"
  }
  greatings();
_________________________________________________
